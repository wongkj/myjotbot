import { createTheme } from '@material-ui/core';

const theme = createTheme({
    palette: {
        primary: {
            main: '#024b30'
        },
        secondary: {
            main: '#d62828'
        },
        neutral: {
            main: '#003049'
        }
    }
})

export default theme;